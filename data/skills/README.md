# Skills

A skill is a folder with a `SKILL.md` entrypoint:

```text
data/skills/<skill-name>/SKILL.md
```

Keep skill instructions focused. Store supporting examples or templates beside the entrypoint only when needed.