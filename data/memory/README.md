# Local Memory

Markdown files in this directory are local-only and ignored by Git. The application
also stores UI-managed private memory under `backend/runtime/user_data/memory`.

Do not put API keys, tokens, passwords, cookies, or other credentials in memory files.
